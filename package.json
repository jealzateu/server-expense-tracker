{
  "name": "json-server-heroku",
  "version": "1.0.0",
  "description": "JSON server for expense tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
